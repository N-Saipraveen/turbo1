# ===================================
# TurboDBX Backend Environment Variables
# ===================================

# Database Connections (Optional - only needed if using direct DB connections)
# For Vercel deployment, you can use cloud database URLs
POSTGRES_URL=postgresql://user:password@localhost:5432/dbname
MYSQL_URL=mysql://user:password@localhost:3306/dbname
MONGODB_URL=mongodb://localhost:27017

# Redis (for job queue) - Optional
# For Vercel, use services like Upstash Redis or Redis Cloud
REDIS_HOST=localhost
REDIS_PORT=6379

# OpenAI Configuration (for AI-enhanced SQL generation)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_ENDPOINT=https://api.openai.com/v1/chat/completions

# Server Configuration
PORT=3000
NODE_ENV=development

# AI Enhancement Settings
ENABLE_AI_ENHANCEMENT=true
VALIDATE_SCHEMA_WITH_AI=true

# ===================================
# Vercel Deployment Notes:
# ===================================
# 1. Add all environment variables in Vercel Dashboard (Settings > Environment Variables)
# 2. Use production database URLs for POSTGRES_URL, MYSQL_URL, MONGODB_URL
# 3. For Redis, consider using Upstash Redis (free tier available)
# 4. Make sure to add your OpenAI API key
# 5. Set NODE_ENV=production in Vercel
# ===================================
